/*


var cook_config = {};
cook_config.workmode = workmode_configs[j];
cook_config.roles = [];

if (cooks[i].russian == 1){
  if (cooks[i].necessityshiftstime == 1){
    if (cooks[i].necessitydayduration == 1){
      for (var hour = 10; hour <= 24 - cooks[i].dayduration; hour++){
        var cook_role = {};
        cook_role.qualification = "russian";
        cook_role.worktime =  hour+" - "+(hour+cooks[i].dayduration);
        cook_config.roles.push(cook_role);

      }
    }
  }
}

full_cooks_configs[i].push(cook_config);*/

//console.log(cooks);
/*var restaurants = new Array(num_of_restaurants);
for (var i = 0; i < num_of_restaurants; i++){
  restaurants[i] = new Array(14);
}*/

/*var generic_rest = new Array(3);
for (var i = 0; i < 3; i++){
  generic_rest[i] = new Array(14);
  for (var j = 0; j < 14; j++){
    generic_rest[i][j] = 0;
  }

  for (k in cooks){
    if (cooks[k].russian == 1){

    }
    console.log(cooks[k].russian);
  }
}*/

/*for (i in cooks){
  full_cooks_configs[i] = [];
  if (cooks[i].workingmode_5_2 == 1){
    full_cooks_configs[i].push(countConfigures(cooks, all_configs_5_2));
  } else {
    full_cooks_configs[i].push(countConfigures(cooks, all_configs_2_2));
  }
}*/
